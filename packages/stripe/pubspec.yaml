name: tekartik_stripe_api
description: A starting point for Dart libraries or applications.
version: 1.0.2
publish_to: none

environment:
  sdk: ^3.5.0

# Add regular dependencies here.
dependencies:
  path: ^1.8.0
  http:
  tekartik_http:
    git:
      url: https://github.com/tekartik/http.dart
      path: http
      ref: dart3a
    version: '>=0.7.5'
  process_run:
  cv: '>=0.2.18+1'
dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
